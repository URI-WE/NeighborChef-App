import 'package:flutter/material.dart';

class FoodShare extends StatelessWidget {
  const FoodShare({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
